-#- content_for :page_title do
-#  = gettext "title translated", :terms.title, instance: site_hostname

.wrapper
  - if @instance_presenter.site_terms.present do
    = @instance_presenter
    -#= raw @instance_presenter.site_terms
  - else
    = gettext "body_html",  :terms.body_html
